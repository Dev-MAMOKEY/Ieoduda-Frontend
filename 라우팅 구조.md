# 기능명세서 기반 라우팅 구조

## 기준

- 이 문서는 `기능명세서` 폴더의 각 문서에 기재된 `진입 조건`과 `사용자 액션`을 기준으로 작성합니다.
- 기능명세서에는 프론트엔드 URL 경로가 정의되어 있지 않으므로 URL은 임의로 지정하지 않습니다.
- 아래 라우팅은 화면 이름과 진입 조건 사이의 관계만 나타냅니다.

## 계획 작성자 기본 진입

```text
앱 최초 진입 또는 로그아웃 상태
└─ 회원가입 / 로그인
   ├─ 계획 없음
   │  └─ 서비스 안내 / 안전 동의
   │     └─ 새 계획 만들기
   │        └─ 삶의 구역 작성
   └─ 계획 있음
      └─ 계획 홈
```

### 분기 기준

- 회원가입 완료 후 계획이 없으면 `서비스 안내 / 안전 동의`로 이동합니다.
- 로그인한 사용자에게 계획이 존재하면 `계획 홈`으로 이동합니다.
- 서비스 안내와 데이터 처리 범위를 확인하면 `새 계획 만들기`로 이동합니다.
- 계획을 생성하면 `삶의 구역 작성`으로 이동할 수 있습니다.

## 계획 작성 및 봉인 흐름

```text
새 계획 만들기
└─ 삶의 구역 작성
   └─ AI 구조화 결과 검토
      └─ 역할 담당자 등록
         ├─ 지정 확인자 등록
         │  └─ 대기·이의 제기 설정
         └─ 충돌 점검 / 발송 순서
            └─ 역할별 패키지 미리보기
               └─ 선택형 생전 인계 점검
```

### 진입 조건

- `삶의 구역 작성`: 계획 생성 후 또는 계획 홈에서 구역을 선택할 때 진입합니다.
- `AI 구조화 결과 검토`: AI 구조화가 완료되면 진입합니다.
- `역할 담당자 등록`: 구조화 항목이 승인되면 진입합니다.
- `지정 확인자 등록`: 계획 작성 중 확인자 설정 단계에서 진입합니다.
- `대기·이의 제기 설정`: 확인자 등록 후 진입합니다.
- `충돌 점검 / 발송 순서`: 담당자와 행동이 등록된 후 진입합니다.
- `역할별 패키지 미리보기`: 역할과 발송 순서가 확정되면 진입합니다.
- `선택형 생전 인계 점검`: 작성자가 봉인된 계획에서 인계 점검을 선택하면 진입합니다.

## 계획 홈 분기

```text
계획 홈
├─ 계획 작성 또는 수정
│  └─ 삶의 구역 작성
├─ 담당자 관리
│  └─ 역할 담당자 등록
├─ 확인자 관리
│  └─ 지정 확인자 등록
├─ 발송 순서
│  └─ 충돌 점검 / 발송 순서
├─ 인계 점검
│  └─ 선택형 생전 인계 점검
├─ 역할별 패키지 확인
│  └─ 역할별 패키지 미리보기
└─ 사건 상태 확인
```

## 설정 및 계정 관리

```text
로그인 상태
└─ 마이페이지
   ├─ 계정 정보 및 이메일 관리
   ├─ 계획 버전 확인
   ├─ 담당자 수락 상태 확인
   ├─ 확인자 수락 상태 확인
   ├─ 대기·이의 제기 설정
   ├─ 계획 비활성화
   ├─ 계정 삭제
   └─ 로그아웃
```

## 생전 역할 수락 흐름

```text
역할 수락 이메일의 만료 링크
└─ 역할 수락 이메일 / 화면
   ├─ 역할 수락
   └─ 역할 거절
```

- 이메일 주소가 변경되면 기존 수락과 링크를 무효화하고 다시 검증합니다.

## 사망 신고 및 증빙 검토 흐름

```text
신고 안내 이메일 또는 확인자 링크
└─ 사망 신고 이메일
   ├─ 확인자 1 독립 신고
   └─ 확인자 2 독립 신고
      ├─ 신고 불일치
      │  └─ 절차 중지 및 운영 검토
      └─ 신고 일치
         └─ 공식 증빙 보안 제출 화면
            └─ 외부 파트너 증빙 검토
               ├─ 반려
               ├─ 추가자료 요청
               └─ 승인
                  └─ 대기 기간 / 이의 제기
```

### 필수 진행 조건

1. 지정 확인자 2명의 독립 신고
2. 두 신고의 대상과 사건 일치
3. 보안 화면을 통한 공식 증빙 제출
4. 외부 법무·장례 파트너 승인
5. 작성자가 선택한 7~30일 대기 기간 종료
6. 작성자 취소 또는 이의 제기 없음

## 대기 및 이의 제기 분기

```text
대기 기간 / 이의 제기
├─ 작성자 취소
│  └─ 전체 절차 취소
├─ 이의 제기 접수
│  └─ 자동 발송 중지 및 수동 검토
└─ 대기 기간 종료 + 취소·이의 제기 없음
   └─ 사후 인계 이메일
```

- 이의 제기가 접수되면 자동 발송을 즉시 중지합니다.
- 분쟁이 해소됐다는 권한 있는 확인 전에는 발송을 재개하지 않습니다.

## 사후 역할별 인계 흐름

```text
대기 기간 종료 + 선행 단계 완료
└─ 사후 인계 이메일
   └─ 만료 링크 확인
      └─ 별도 이메일 OTP 확인
         └─ 역할별 사후 패키지
            ├─ 행동 완료
            │  └─ 단계 완료 / 대체 담당자
            └─ 문제 신고
               └─ 단계 완료 / 대체 담당자
```

### 단계 분기

```text
단계 완료 / 대체 담당자
├─ 단계 완료
│  └─ 다음 단계 활성화
├─ 최대 대기 초과 또는 영구 반송
│  ├─ 사전 수락한 대체 담당자 있음
│  │  └─ 대체 담당자에게 이메일 발송
│  └─ 대체 담당자 없음
│     └─ 사건 차단 상태 유지
└─ 문제 신고
   └─ 다음 단계 잠금 유지
```

## 운영자 및 외부 파트너 화면

```text
권한 있는 운영자
├─ 이메일 발송 감사
│  ├─ 재시도 정책 실행
│  ├─ 사건 동결
│  └─ 파트너 문의
└─ 증빙 삭제 감사
   └─ 자동 삭제 실패 시 재처리 요청

인증된 외부 파트너
└─ 외부 파트너 증빙 검토
   ├─ 승인
   ├─ 반려
   └─ 추가자료 요청
```

## 증빙 삭제 흐름

```text
외부 파트너 검토 완료
└─ 증빙 삭제 일정 생성
   ├─ 검토 완료 후 30일 이내 원본 삭제
   ├─ 삭제 완료
   │  └─ 삭제 감사 정보 유지
   └─ 삭제 실패
      └─ 증빙 삭제 감사에서 재처리 요청
```

- 삭제 후 원본 미리보기는 제공하지 않습니다.
- 감사 기록에는 원본이 아닌 무결성 해시와 삭제 관련 시각을 유지합니다.
